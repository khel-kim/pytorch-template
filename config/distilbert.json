{
  "env": {
    "seed": 0,
    "using_wandb": "F"
  },
  "model": {
    "name": "distilbert",
    "size": "base",
    "loss": "cce",
    "opt": "adam",
    "pretrained_model_path": "F"
  },
  "training": {
    "lr": 1e-6,
    "epochs": 5,
    "batch_size": 8,
    "max_len": 128
  }
}